import { Text, SafeAreaView } from "react-native";

const Profile = () => {
  return (
    <SafeAreaView
      style={{
        paddingHorizontal: 10,
      }}
    >
      <Text>profile</Text>
    </SafeAreaView>
  );
};

export default Profile;
